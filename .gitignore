# local data — never push annotation work, audit trails, or exports
annotations/
exports/
Output/
videos/
*.audit.log
.audit.log

# keys — the private key must NEVER enter this repo
*.pem
*.key

# build junk
venv/
dist/
build/
*.spec
__pycache__/
